import 'package:flutter/material.dart';

class GlobalNewsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Global News'),
      ),
      body: Center(
        child: Text('Global News Sayfası'),
      ),
    );
  }
}
